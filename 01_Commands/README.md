# Linux Commands

- Create a shorcut:
    - `ln -s /path/to/target ~/shortcut_name`

- Kill a process
    - `kill -9 <PID>`

- To check all linked libraries with a file
    - `ldd <file_name>` 